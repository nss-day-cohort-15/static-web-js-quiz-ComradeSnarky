  treeObj {
    height: document.getElementbyId("height"),
    character: document.getElementbyId("character")
  }

//creates a tree w/ height and charater provided by user



//Specifies character for the tree



//if enter is pressed the event listener will run
//this function to grow tree




//Gives alert if text field is not filled in.
//Otherwise it runs the tree program





//Button event listener to grow tree
document.getElementbyId("initializeTree").addEventListener("click", //(need function to stop if input is blank))


//Enter event listener to grow tree
document.getElementbyId("height").addEventListener("keypress", //need function to stop if height input is blank)
document.getElementbyId("character").addEventListener("keypress", //need function to stop if character input is blank)